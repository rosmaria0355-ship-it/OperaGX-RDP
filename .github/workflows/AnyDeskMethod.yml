name: Cloud PC + AnyDesk + Opera GX (Unattended Fix Layout)

on:
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    shell: pwsh

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 190
    env:
      ANYDESK_PASS: Pass@1234
      RUNTIME_MINUTES: 180

    steps:
      - name: Wipe Unnecessary Pre-Installed GitHub Apps & Shortcuts
        run: |
          Write-Host "Purging pre-installed desktop icons and non-essential software..."
          $publicDesktop = [Environment]::GetFolderPath("CommonDesktopDirectory")
          $userDesktop = "$env:USERPROFILE\Desktop"
          
          $targets = @("Unity Hub.lnk", "R 4.6.0.lnk", "Firefox.lnk", "Google Chrome.lnk", "Claim Rewards Here.lnk")
          foreach ($target in $targets) {
              if (Test-Path "$publicDesktop\$target") { Remove-Item "$publicDesktop\$target" -Force -ErrorAction SilentlyContinue }
              if (Test-Path "$userDesktop\$target") { Remove-Item "$userDesktop\$target" -Force -ErrorAction SilentlyContinue }
          }

      - name: Download and Force Install AnyDesk
        run: |
          $url = "https://download.anydesk.com/AnyDesk.exe"
          $dst = "$env:TEMP\AnyDesk.exe"
          Invoke-WebRequest -Uri $url -OutFile $dst
          
          $installDir = "C:\Program Files (x86)\AnyDesk"
          if (-not (Test-Path $installDir)) { New-Item -ItemType Directory -Path $installDir -Force | Out-Null }

          Start-Process $dst -ArgumentList "--install `"$installDir`"", "--silent", "--start-with-win" -Wait
          Start-Sleep -Seconds 5
          Remove-Item $dst -ErrorAction SilentlyContinue

      - name: Configure Unattended Access Password & Hide Tray Apps
        run: |
          $ad = "C:\Program Files (x86)\AnyDesk\AnyDesk.exe"
          if (-not (Test-Path $ad)) { $ad = "C:\Program Files\AnyDesk\AnyDesk.exe" }
          
          cmd.exe /c "echo ${{ env.ANYDESK_PASS }} | `"$ad`" --set-password"
          
          $path = "HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\Advanced"
          Set-ItemProperty -Path $path -Name "TaskbarGlomLevel" -Value 0 -ErrorAction SilentlyContinue
          
          Start-Process -FilePath $ad -ArgumentList "--restart-service" -Wait
          Start-Sleep -Seconds 5

      - name: Clean and Hide Active Infrastructure Windows
        run: |
          Write-Host "Minimizing workspace windows natively..."
          (New-Object -ComObject Shell.Application).MinimizeAll()

      - name: Optimize Resolution for Mobile Display
        run: |
          Write-Host "Forcing resolution calibration matrix..."
          Set-DisplayResolution -Width 1280 -Height 720 -Force -ErrorAction SilentlyContinue

      - name: Deploy Stable Opera GX via Package Repository
        run: |
          Write-Host "Configuring Chocolatey engine to suppress all prompt behaviors..."
          # Forces the engine layer to accept all licenses and scripts automatically before executing
          choco feature enable -n=allowGlobalConfirmation
          
          Write-Host "Deploying verified package target via repository management..."
          choco install opera-gx --yes --no-progress --accept-license
          
          $publicDesktop = [Environment]::GetFolderPath("CommonDesktopDirectory")
          $userDesktop = "$env:USERPROFILE\Desktop"
          
          if (Test-Path "$userDesktop\Opera GX.lnk") { Remove-Item "$userDesktop\Opera GX.lnk" -Force }
          if (Test-Path "$publicDesktop\Opera GX.lnk") { Remove-Item "$publicDesktop\Opera GX.lnk" -Force }

          Write-Host "Configuring launch keys to suppress onboarding..."
          $regPath = "HKLM:\SOFTWARE\Policies\Opera"
          if (-not (Test-Path $regPath)) { New-Item -Path $regPath -Force | Out-Null }
          Set-ItemProperty -Path $regPath -Name "HideFirstRunUI" -Value 1 -ErrorAction SilentlyContinue

          Write-Host "Configuring launch arguments to force rewards configuration..."
          $operaExe = "C:\Program Files\Opera GX\opera.exe"
          if (-not (Test-Path $operaExe)) { $operaExe = "C:\Program Files (x86)\Opera GX\opera.exe" }
          
          $WshShell = New-Object -ComObject WScript.Shell
          $Shortcut = $WshShell.CreateShortcut("$publicDesktop\Opera GX Browser.lnk")
          $Shortcut.TargetPath = $operaExe
          $Shortcut.Arguments = "--no-first-run --partner-id=forsaken --enable-sidebar-extensions --enable-features=gxa-rewards,GXRewards,GXRewardsSidebarItem --with-feature:gx-rewards=1"
          $Shortcut.Save()
          Write-Host "Shortcut generated successfully."

      - name: Fetch and Display AnyDesk Connection Address
        run: |
          $confPath = "C:\ProgramData\AnyDesk\system.conf"
          $cleanId = ""
          
          for ($i = 1; $i -le 10; $i++) {
              if (Test-Path $confPath) {
                  $content = Get-Content $confPath -Raw
                  if ($content -match 'ad\.anynet\.id=(\d+)') {
                      $cleanId = $Matches[1]
                      break
                  }
              }
              Start-Sleep -Seconds 4
          }
          
          $ESC = [char]27
          $Green = "$ESC[32m"
          $Yellow = "$ESC[33m"
          $Red = "$ESC[31m"
          $Reset = "$ESC[0m"
          
          Write-Host "${Green}====================================================${Reset}"
          Write-Host "${Green}       ANYDESK CLOUD MACHINE IS READY TO CONNECT     ${Reset}"
          Write-Host "${Green}====================================================${Reset}"
          Write-Host "${Yellow} CONNECTION ID : ${Red}${cleanId}${Reset}"
          Write-Host "${Yellow} PASSWORD      : ${Red}${{ env.ANYDESK_PASS }}${Reset}"
          Write-Host "${Green}====================================================${Reset}"

      - name: Keep alive
        run: |
          $mins = [int]"${{ env.RUNTIME_MINUTES }}"
          $end = (Get-Date).AddMinutes($mins)
          while ((Get-Date) -lt $end) {
            Write-Host ("Cloud PC Heartbeat " + (Get-Date).ToString("HH:mm:ss") + " | Active until " + $end.ToString("HH:mm:ss"))
            Start-Sleep -Seconds 60
          }
